% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rasterise_plot.R
\name{rasterise_plot}
\alias{rasterise_plot}
\title{Rasterise the layers of a plot}
\usage{
rasterise_plot(
  plot,
  layers = c("Point", "Tile", "Path", "Line", "Segment"),
  dpi = 300,
  dev = "ragg",
  recurse = TRUE
)
}
\arguments{
\item{plot}{A \code{ggplot} or \code{patchwork} object.}

\item{layers}{Layer types to rasterise, named without the \code{Geom} prefix, so
the default \code{"Point"} matches \code{geom_point()} layers. Add \code{"Violin"},
\code{"Boxplot"} or \code{"Crossbar"} to rasterise those layers as well.}

\item{dpi}{Resolution of the rasterised layers, in dots per inch.
Default is \code{300}.}

\item{dev}{Graphic device used to render the rasterised layers, passed to
\code{\link[ggrastr:rasterise]{ggrastr::rasterise()}}. Default is \code{"ragg"}, which does not need X11; the
\code{"cairo"} device of the underlying package does, and is unavailable on many
headless machines.}

\item{recurse}{Whether to rasterise the sub-plots of a \code{patchwork}
composite. Default is \code{TRUE}.}
}
\value{
The plot object with its heavy layers rasterised. Objects that are
neither a \code{ggplot} nor a \code{patchwork} are returned unchanged, with a warning.
}
\description{
Render the point and line layers of a \code{ggplot} or \code{patchwork} object as
raster images, while the axes, text, legends and the remaining layers stay
vector. A figure with a large number of elements then stays small and quick
to draw, in the file and in the browser. For a \code{patchwork} composite the
sub-plots are rasterised as well, because a composite carries only the first
sub-plot's layers itself. The rasterisation is done by
\code{\link[ggrastr:rasterise]{ggrastr::rasterise()}}.
}
\examples{
panel <- ggplot2::ggplot(mtcars, ggplot2::aes(wt, mpg)) +
  ggplot2::geom_point() +
  ggplot2::facet_wrap(~cyl)

rasterise_plot(panel)

rasterise_plot(patchwork::wrap_plots(panel, panel), dpi = 150)

}
\seealso{
\code{\link[=invert_svg]{invert_svg()}}
}
