# Estimate the fuzzifier parameter m

Estimate the fuzzifier parameter m for fuzzy clustering based on the
data dimensions.

## Usage

``` r
mestimate(data)
```

## Arguments

- data:

  A matrix or data frame.

## Value

The estimated fuzzifier parameter m.
