% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_autosize.R
\name{plot_autocompose}
\alias{plot_autocompose}
\title{Compose plots at their preferred sizes}
\usage{
plot_autocompose(
  plots,
  ncol = NULL,
  nrow = NULL,
  panel_width = 45,
  panel_height = 35,
  margin = 2,
  units = "mm",
  save = NULL,
  dpi = 300,
  base_size = "auto",
  label_autosize = TRUE,
  target_aspect = 1.6,
  target_width = NULL,
  layout = c("publication", "rows"),
  gutter = 4
)
}
\arguments{
\item{plots}{A plot or list of plots accepted by \code{\link[=plot_autosize]{plot_autosize()}}.}

\item{ncol, nrow}{Number of columns and rows. Supplying either uses the ordered
row layout.}

\item{panel_width, panel_height}{Base panel width and height.}

\item{margin}{Outer margin.}

\item{units}{Units for panel dimensions and margin. See \code{\link[grid:unit]{grid::unit()}}.}

\item{save}{\code{NULL} or a file name to save the fitted plot.}

\item{dpi}{Resolution used when saving raster output.}

\item{base_size}{\code{"auto"} to harmonize ggplot and grid text to one inferred
base size in points, a positive point size, or \code{NULL} to preserve each
component's typography.}

\item{label_autosize}{Whether to enlarge constant \code{geom_text}, \code{geom_label},
and ggrepel label sizes when the panel has enough horizontal space.}

\item{target_aspect}{Preferred overall width-to-height ratio when \code{ncol} and
\code{nrow} are both \code{NULL}.}

\item{target_width}{Optional publication target width in \code{units}. Limits canvas
width to fit journal single-column (e.g. 85 mm) or double-column (e.g. 180 mm) constraints.}

\item{layout}{\code{"publication"} for compact asymmetric placement or \code{"rows"}
to preserve input order in rows.}

\item{gutter}{Space between components in \code{units} for the publication layout.}
}
\value{
A wrapped plot with \code{size} and \code{autosize} attributes. Component sizes
are stored in \code{attr(x, "autosize")$components}.
}
\description{
Calculate each plot's preferred physical size and keep every plot at that
size. The default publication layout searches compact asymmetric placements
with aligned edges, while the ordered layout packs shorter rows continuously
and centers them. Apply plot-specific styling before composing.
}
\examples{
p1 <- ggplot2::ggplot(mtcars, ggplot2::aes(wt, mpg)) +
  ggplot2::geom_point()
p2 <- p1 + ggplot2::labs(title = "A wider plot title")
plot_autocompose(list(p1, p2), ncol = 2)
}
