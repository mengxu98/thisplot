% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_autosize.R
\name{plot_autosize}
\alias{plot_autosize}
\title{Automatically size a plot}
\usage{
plot_autosize(
  x,
  panel_width = 45,
  panel_height = 35,
  margin = 2,
  units = "mm",
  save = NULL,
  dpi = 300,
  base_size = "auto",
  label_autosize = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{x}{A ggplot object, grob, or combined plot accepted by \code{\link[=panel_fix]{panel_fix()}}.}

\item{panel_width, panel_height}{Base panel width and height.}

\item{margin}{Outer margin.}

\item{units}{Units for panel dimensions and margin. See \code{\link[grid:unit]{grid::unit()}}.}

\item{save}{\code{NULL} or a file name to save the fitted plot.}

\item{dpi}{Resolution used when saving raster output.}

\item{base_size}{\code{"auto"} to harmonize ggplot and grid text to one inferred
base size in points, a positive point size, or \code{NULL} to preserve each
component's typography.}

\item{label_autosize}{Whether to enlarge constant \code{geom_text}, \code{geom_label},
and ggrepel label sizes when the panel has enough horizontal space.}

\item{verbose}{Whether to report messages from \code{\link[=panel_fix]{panel_fix()}}.}
}
\value{
A plot with \code{size} and \code{autosize} attributes. Ordinary plots are
returned through \code{\link[=panel_fix]{panel_fix()}}; wrapped grid content is preserved directly.
}
\description{
Build a compact plot by choosing a physical panel size and expanding it for
dense in-panel text or overflowing plot labels. A shared point-size baseline
harmonizes ggplot, patchwork, and recorded grid text such as wrapped
ComplexHeatmap output while preserving their relative hierarchy. Constant
data-label text is enlarged conservatively when space permits. The
calculation delegates final layout and saving to
\code{\link[=panel_fix]{panel_fix()}} while preserving label content, fixed aspect ratios, patchwork
layouts, and that function's behaviour.
}
\examples{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg, colour = factor(cyl))) +
  geom_point()
p1 <- p +
  labs(title = "Fuel economy by vehicle weight")
p1

p2 <- plot_autosize(p)
p2

attr(p2, "size")

plot_autosize(p1)
}
