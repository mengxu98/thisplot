% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/invert_figures.R
\name{invert_figures}
\alias{invert_figures}
\title{Invert the colours of a set of figures}
\usage{
invert_figures(
  path,
  output_dir = NULL,
  suffix = "-dark",
  recursive = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{path}{Path(s) to figure files, or to directories holding them.}

\item{output_dir}{Directory the dark variants are written to. Default is
next to each input file.}

\item{suffix}{Suffix added to the file name of every dark variant. Default
is \code{-dark}.}

\item{recursive}{Whether a directory is scanned recursively. Default is
\code{FALSE}.}

\item{quiet}{Whether to suppress the per-file messages. Default is \code{FALSE}.}
}
\value{
The paths of the written files, invisibly.
}
\description{
Build dark-mode variants of several figures at once, dispatching on the file
extension: SVG files are inverted with \code{invert_svg()} and PNG files with
\code{invert_png()}. Every output is written next to its input, or into
\code{output_dir}, with a \code{-dark} suffix added to the name. Files that already
carry the suffix are skipped, so running the function twice does not
complement the dark variants again.
}
\details{
Other formats are not supported: there is no way to rewrite the colours of a
PDF from R without rasterising it first. Re-render the figure as SVG or PNG
where possible, for instance with \code{ggsave()}, or rasterise the PDF before
inverting it. Unsupported files are only reported when they are passed as a
file; a directory is scanned for the supported extensions.
}
