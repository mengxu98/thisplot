% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_scatter.R
\name{plot_scatter}
\alias{plot_scatter}
\title{Scatter plot}
\usage{
plot_scatter(
  data,
  smoothing_method = c("lm", "loess"),
  palette = "Chinese",
  palcolor = NULL,
  title = NULL,
  xlab = NULL,
  ylab = NULL,
  legend.title = NULL,
  legend.position = "right",
  theme_use = "theme_this",
  theme_args = list(),
  margins = c("both", "x", "y"),
  marginal_type = NULL,
  margins_size = 10,
  compute_correlation = TRUE,
  compute_correlation_method = c("pearson", "spearman"),
  keep_aspect_ratio = TRUE,
  facet = FALSE,
  se = FALSE,
  pointdensity = TRUE
)
}
\arguments{
\item{data}{A data frame with 2 columns (\code{x}, \code{y}) or 3 columns
(\code{x}, \code{y}, and a grouping column). Extra columns are ignored.}

\item{smoothing_method}{Smoothing method passed to \link[ggplot2:geom_smooth]{ggplot2::geom_smooth}.
Can be \code{"lm"} or \code{"loess"}.}

\item{palette}{Palette name used for groups or point-density coloring.
See \link{show_palettes}.}

\item{palcolor}{Custom colors used to create a color palette.}

\item{title, xlab, ylab}{Plot titles.}

\item{legend.title}{Legend title.}

\item{legend.position}{Legend position.}

\item{theme_use}{Theme function applied to the plot.}

\item{theme_args}{A named list of arguments passed to \code{theme_use}.}

\item{margins, marginal_type, margins_size}{Marginal plot controls used when
\code{ggExtra} is installed. \code{marginal_type} can be \code{"density"}, \code{"histogram"},
\code{"boxplot"}, \code{"violin"}, or \code{"densigram"}.}

\item{compute_correlation}{Whether to annotate Pearson or Spearman correlation.}

\item{compute_correlation_method}{Correlation method: \code{"pearson"} or \code{"spearman"}.}

\item{keep_aspect_ratio}{Whether to use a 1:1 aspect ratio.}

\item{facet}{Whether to facet by the grouping column when \code{data} has 3 columns.}

\item{se}{Whether to show smoothing uncertainty.}

\item{pointdensity}{Whether to color ungrouped points by local density when
\code{ggpointdensity} is installed.}
}
\value{
A ggplot object, or a \code{ggExtra} grob when marginal plots are added.
}
\description{
Plot two- or three-column data as a scatter plot with \code{theme_this} and
thisplot palettes. Optional smoothing, correlation annotation, point density,
and marginal plots are available.
}
\examples{
set.seed(1)
plot_scatter(data.frame(x = rnorm(80), y = rnorm(80)))
plot_scatter(
  data.frame(
    x = rnorm(80),
    y = rnorm(80),
    cluster = rep(c("A", "B"), each = 40)
  )
)
}
